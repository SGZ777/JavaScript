let numeros = [1, 2, 3, 4, 5, 6];

function multiplicar(array) {
  return array.map(n => n * 2);
}
// map funciona como um remapeamento da variavel, ele pega o valor dela e remodela de acordo com o que o usuário desejar